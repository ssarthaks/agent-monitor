{
  "name": "agent-monitor-monorepo",
  "version": "0.1.5",
  "private": true,
  "description": "Agent Monitor - Chrome DevTools / Activity Monitor for AI Agents",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:web": "npm run build --workspace=@agent-monitor/web && mkdir -p packages/server/public && cp -R apps/web/out/* packages/server/public/",
    "build": "npm run build:web && npm run build --workspaces --if-present",
    "test": "vitest run",
    "dev:web": "npm run dev --workspace=apps/web",
    "cli": "tsx packages/cli/src/index.ts",
    "publish:packages": "npm publish --workspace=@agent-monitor/core --workspace=@agent-monitor/server --workspace=@agent-monitor/agent --workspace=@agent-monitor/cli --access public"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
